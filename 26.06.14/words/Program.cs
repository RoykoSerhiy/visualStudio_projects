using System;
using System.Collections.Generic;
using System.Linq;


namespace words
{
    class Program
    {
        static void Main()
        {
            string txt = "If you're paying attention, you might have noticed the other addition to the code - the Action class. This is because the Invoke and BeginInvoke methods only take delegates, so simply using a lambda expression won't cut it. The Action class wraps anonymous methods in a delegate, allowing their use in cases such as this. The more you know, and such";
            
        }
    }
}
